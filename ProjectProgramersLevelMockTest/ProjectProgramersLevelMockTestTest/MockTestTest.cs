using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace ProjectProgramersLevelMockTestTest
{
    [TestClass]
    public class MockTestTest
    {
        // 수포자는 A,B,C이다
        // 맞춘 개수는 N개이다.
        // 제일 많이 맞춘 수포자만을 등록. 만일 모두 동일하게 맞췄다면 오름차순으로 리스트를 뿌려준다.
        // 배열의 원소는 1~5까지이다.
        // answers는 10000이하이다.


        [TestMethod]
        public void 배()
        {
        }
    }
}
